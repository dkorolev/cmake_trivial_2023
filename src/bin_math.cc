#include <iostream>
#include "lib.h"

int main() {
  std::cout << "The answer is: " << mul(6, 7) << std::endl;
  std::cout << "Also, " << add(2, 3) << " and " << mul(2, 3) << " are 5 and 6 respectively." << std::endl;
}
